@page "/Clients/Delete/{Id:int}"
@inject HttpClient http
@inject NavigationManager navManager

<h1 class="mb-3">Deletar Cliente</h1>

<p>Tem certeza que quer deletar esse cliente com o @Id?</p>

<button class="btn btn-danger" @onclick="DeleteClient">Deletar</button>
<a class="btn btn-secondary" href="/Clients">Cancel</a>

@code {
    [Parameter]
    public int Id { get; set; }

    protected async Task DeleteClient()
    {
        var response = await http.DeleteAsync("https://localhost:4000/api/Clients/" + Id);
        navManager.NavigateTo("/Clients");
    }
}
